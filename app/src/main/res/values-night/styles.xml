<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.Dialog.Alert">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">#111111</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorPrimary">#FF00BEAB</item>
        <item name="noti_text_color">@android:color/darker_gray</item>
    </style>
</resources>
